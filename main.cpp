#include <iostream>
#include "habit_manager.h"

int main() {
    HabitManager habitManager;

    std::cout << "Bienvenido al Gestor de Hábitos!" << std::endl;

    // Aquí se pueden agregar interacciones con el usuario para gestionar hábitos
    // Ejemplo: agregar, eliminar y listar hábitos

    return 0;
}